[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "engramd"
version = "0.1.0"
description = "Python client for the Engram Memory Vault"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
    "sseclient-py>=1.8",
]

[project.optional-dependencies]
mcp = ["mcp>=1.0"]

[project.scripts]
engramd-mcp = "engramd.mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/engramd"]
